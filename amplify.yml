version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd front-end
        - yarn install
    build:
      commands:
        - yarn build
  artifacts:
    baseDirectory: front-end/build
    files:
      - '**/*'
  cache:
    paths:
      - front-end/node_modules/**/*
