import React from 'react'
import { Divider } from '@mui/material'

// Divider의 속성을 정리해놓은 컴포넌트

const DivLine = () => {
  return (
    <Divider sx={{ borderColor: '#012A5B', mt: 3, mb: 3 }} />
  )
}

export default DivLine